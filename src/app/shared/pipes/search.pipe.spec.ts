import { SearchPipe } from './search.pipe';

describe('SearchPipe', () => {
  it('create an instance of SearchPipe', () => {
    const pipe = new SearchPipe();
    expect(pipe).toBeTruthy();
  });
});
